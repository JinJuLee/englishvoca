node_modules/
dist/
.DS_Store
.env
.env.local
*.log
.vite/
.claude/

# Source PDFs (too large for git, kept locally)
*.pdf

# All vocabulary data stays local — copyright stays with original publishers.
# Only the schema example under data/example/ is committed.
data/*/
!data/example/
